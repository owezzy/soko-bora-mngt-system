import { Route } from '@angular/router';
import {Auth0Component} from './auth0.component';

export const auth0Routes: Route[] = [
    {
        path     : '',
        component: Auth0Component
    }
];
